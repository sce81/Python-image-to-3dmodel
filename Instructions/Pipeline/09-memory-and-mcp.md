# Memory and MCP

Before changing a run configuration, query `memory_mcp_server.py` for matching asset class and stage. A matching bad memory requires a changed method, not another seed.

Record reviewed good/bad evidence after validation. Never delete memory entries; supersede them. Keep `mcp_server.py` thin and preserve LAN-only streamable HTTP with the existing token gate.
