# Pipeline instructions

Use the stage documents in `Pipeline/` with `AGENTS.md`. They define the supported local Windows runtime, source conditioning, geometry generation, validation, Blender release, memory/MCP behavior, and optional AWS G7 execution.
